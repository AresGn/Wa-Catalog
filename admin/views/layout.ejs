<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - wa-catalog Admin</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <nav class="navbar">
    <div class="container">
      <div class="logo">
        <h1>🚀 wa-catalog Admin</h1>
      </div>
      <ul class="nav-links">
        <li><a href="/" class="<%= title === 'Dashboard' ? 'active' : '' %>">Dashboard</a></li>
        <li><a href="/vendors" class="<%= title === 'Vendors' ? 'active' : '' %>">Vendors</a></li>
        <li><a href="/products" class="<%= title === 'Products' ? 'active' : '' %>">Products</a></li>
      </ul>
    </div>
  </nav>

  <main class="container">
    <% if (typeof message !== 'undefined') { %>
      <div class="alert alert-success"><%= message %></div>
    <% } %>
    <% if (typeof error !== 'undefined') { %>
      <div class="alert alert-error"><%= error %></div>
    <% } %>

    <%- body %>
  </main>

  <footer class="footer">
    <p>&copy; 2024 wa-catalog. All rights reserved.</p>
  </footer>

  <script src="/js/main.js"></script>
</body>
</html>
